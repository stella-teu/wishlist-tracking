<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
</head>
<body>
    <% if (user){ %>
        <h1>Welcome to the Wishlist Tracker, <%= user.username %></h1>
        <a href="users/<%= user._id %>/wishlist">View wishlist</a>
        <a href="/community">Community</a>
        <a href="/auth/sign-out">Sign Out</a>
    <% } else { %>
        <h1>Welcome to the Wishlist Tracker, guest.</h1>
    <a href="/auth/sign-up">Sign up</a>
    <a href="/auth/sign-in">Sign in</a>
    <% } %>
</body>
</html>