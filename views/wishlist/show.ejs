<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title><%= item.name %></title>
</head>
<nav><a href="/">Home</a> 
    <a href="/users/<%= user._id %>/wishlist">View Wishlist</a> 
    <a href="/community">Community</a>
    <a href="/auth/sign-out">Sign Out</a>
</nav>
<body>
    <h1><%= item.name %></h1>
    <button><a href="/users/<%= user._id %>/wishlist/<%= item._id %>/edit">Edit</a>
    </button>
    <form action="/users/<%= user._id %>/wishlist/<%= item._id %>?_method=DELETE" method="POST">
        <button type="submit">Delete</button>
    </form>
    <% if (item.price) { %>
    <p>Price: $<%= item.price %></p>
    <% } else { %>
        <p>Price not added.</p>
        <% } %>
    <% if (item.description) { %>
    <p>Description: <%= item.description %></p>
    <% } else { %>
        <p>Description not added.</p>
        <% } %>
    <% if (item.link) { %>
    <p><a href="<%= item.link %>">Link</a></p>
    <% } else { %>
        <p>Link not added.</p>
        <% } %>
</body>
</html>