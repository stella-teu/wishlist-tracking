<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Edit <%= item.name %></title>
</head>
<body>
    <nav>
        <div>
        <button class="nav"><a href="/" class="nav">Home</a></button>
        <button class="nav"><a href="/users/<%= user._id %>/wishlist" class="nav">View Wishlist</a></button>
        <button class="nav"><a href="/community" class="nav">Community</a></button>
    </div>
    <div>
        <button class="nav"><a href="/auth/sign-out" class="nav">Sign Out</a></button>
    </div>
    </nav>
    <section class="form">
    <h1>Edit <%= item.name %></h1>
    <form action="/users/<%= user._id %>/wishlist/<%= item._id %>?_method=PUT" method="POST">
        <label for="name">Name: </label>
        <input type="name" name="name" id="name" value="<%= item.name %>" required />
        <label for="price">Price: </label>
        <input type="price" name="price" id="price" value="<%= item.price %>"/>
        <label for="description">Description: </label>
        <textarea name="description" id="description"><%= item.description %></textarea>
        <label for="link">Link: </label>
        <input type="link" name="link" id="link" value="<%= item.link %>"/>
        <button type="submit" id="submit">Submit Change</button>
    </form>
</section>
</body>
</html>