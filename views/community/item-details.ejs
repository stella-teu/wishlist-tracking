<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title><%= item.name %> details</title>
</head>
<body>
    <nav>
        <div>
        <button class="nav"><a href="/" class="nav">Home</a></button>
        <button class="nav"><a href="/users/<%= user._id %>/wishlist" class="nav">View Wishlist</a></button>
        <button class="nav"><a href="/community" class="nav">Community</a></button>
    </div>
    <div>
        <button class="nav"><a href="/auth/sign-out" class="nav">Sign Out</a></button>
    </div>
    </nav>
    <section class="header">
    <h1><%= item.name %></h1>
</section>
<main class="show">
    <div class="show">
    <% if (item.price) { %>
        <p>Price: $<%= item.price %></p>
        <% } else { %>
            <p>Price not added.</p>
            <% } %>
        </div>
        <div class="show">
        <% if (item.description) { %>
        <p>Description: <%= item.description %></p>
        <% } else { %>
            <p>Description not added.</p>
            <% } %>
        </div>
        <div class="show">
        <% if (item.link) { %>
        <p><a href="<%= item.link %> " target="_blank" id="link">Link</a></p>
        <% } else { %>
            <p>Link not added.</p>
            <% } %>
        </div>
        <div class="show">
    <p><a href="/community/show/<%= watchedUser._id %>" id="link">Return to <%= watchedUser.username %>'s Wishlist</a></p>
</div>
</main>
</body>
</html>