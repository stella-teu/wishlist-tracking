<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Community</title>
</head>
<body>
    <nav>
        <div>
        <button class="nav"><a href="/" class="nav">Home</a></button>
       <button class="nav"><a href="/users/<%= user._id %>/wishlist" class="nav">View Wishlist</a></button>
    </div>
    <div>
        <button class="nav"><a href="/auth/sign-out" class="nav">Sign Out</a></button>
    </div>
    </nav>
    <section class="header">
    <h1>Community</h1>
</section>
<main>
        <% users.forEach(user => { %>
            <button id="item"><a href="/community/show/<%= user._id %>"><%= user.username %></a></button>
        <% }); %>
</main>
</body>
</html>